# Kyberia implementation status

The authoritative specification is [plan.md](plan.md). This is an active implementation, not a completed product.

- Current phase: **Phase 0 — Research harness and architecture proof**.
- Current iteration: **4 — Integration audit remediation, RF semantics, renderer/geometry proofs and supply-chain validation**.
- Completed foundation increments: independently reviewed immutable spatial-analysis manifests, canonical hashes and artifact-byte verification; independently reviewed lossless source ledger and execution DAG; unit-safe IDs/physical values, explicit unknowns, timing, covariance and observation contracts; 24 original scientific scenes, 25-point deterministic survey, clean-room TIN oracle and runtime evidence checker. These do not constitute a usable survey application or completion of Phase 0.
- In progress: Complete distributable supply-chain coverage, renderer comparison, geometry-kernel comparison, explicit Wi-Fi signal aggregation/power arithmetic, and remediation of the iteration-wide independent audit. That audit found finite PCAPNG section-length parsing, SQLite sidecar budgeting, and native macOS-to-point-survey admission defects; isolated fixes and independent reviews are active. It also found spatial aggregation-provenance drift, which remains queued. The pinned Rust CLI SBOM/advisory tooling is independently approved and its committed-source macOS ARM64 execution passes; Python/Sionna/Kismet/controller and other host distributions remain open. The bounded active-process research boundary is independently approved after lifecycle and UDP-source corrections. Native capture normalization is independently approved after two API-timing corrections; the midpoint audit’s silent storage-write defect is corrected and independently verified. The isolated Sionna CPU proof and source-response timing contract are independently approved. The real SQLite/Parquet/DuckDB comparison is now independently approved after its required-provenance correction. Independently approved increments now include the point-survey state machine, immutable KismetDB metadata reader, bounded PCAPNG container adapter, numerical nearest/IDW tiles, observation/snapshot V2 migration, native macOS process, dependency checks and developer commands. These remain backend/proof increments; user-facing survey and canonical adapter wiring are open. Every agent has an isolated worktree within `.worktrees/`.
- Blocked capabilities: no whole capability is classified blocked. Specific unavailable hardware/OS/credential validation gates will be recorded separately in [BLOCKERS.md](docs/implementation/BLOCKERS.md).
- Validation status: `python3 tools/dev.py check` passes: 172 Rust tests (19 capture, 60 domain, 23 survey, 12 KismetDB, 17 PCAPNG, 16 spatial, 23 storage, two executable CLI workflows), seven compile-fail doctests, 149 Python tests with 19 optional native/analytical-engine/real-tool tests skipped, formatting, Clippy, typecheck, architecture, source and ledger checks. Six explicit Rust benchmarks and a deliberate golden-regeneration command are skipped in the default run; benchmarks pass when invoked separately. The separate native macOS command passes all 21 collector tests including those five native lifecycle tests. The optional real storage environment separately passes all 15 tests and nine benchmark cases. The source checker verifies 97 locked external packages. The committed-source Rust CLI SBOM contains 45 components and 46 dependency nodes, is byte-identical on repeat generation, validates against pinned official CycloneDX schemas, and passes cargo-deny advisories/licenses/bans/sources against RustSec `8a1eb4f`. Separately, the Sionna audited CPU environment passes 34 author checks and 19 independently rerun upstream tests; independent asymmetric path/map probes pass. The complete Sionna runtime gate remains open. Actual redacted CoreWLAN probing detects one interface outside the sandbox; authorized RF measurements remain open pending operator consent and field validation.
- Latest reviewed commit: `f834629` pinned SBOM and dependency-audit tooling, independently approved with committed-source evidence in the following validation increment; `7d9022c` bounded active process proof; `cddd37b` immutable analysis manifests. Prior increments: `d6189d6` native capture normalization, independently approved in `21cd7e7`; storage correction `8f83a2f`, independently approved in `0d0ff97`; `b791ba0` Sionna CPU proof, independently approved in `7a10bb1`; source-response timing `f227b8b`, independently approved in `25b8249`; `6db6058` real storage comparison, independently approved in `24c2fe5` with separate correction review `86b860a`. Packet adapter `1eea541` is approved in `8ca1575` after correcting final-EOF cancellation. Spatial numerical code `c707ba3` is independently approved in `fc76fd7`.
- Known technical debt: native managed-mode observations lack an admitted survey timing policy; spatial-analysis metric provenance does not yet constrain its coincident dBm aggregation. Individual manifest metadata hash-mutation cases need broader regression coverage; full editor undo/merge and large-history replay remain open; metadata operation replay currently grows quadratically. Renderer, storage, capture, geometry, optimizer and integration runtime gates are unresolved. Source IDs collide between sections and must never be used unqualified.
- Next executable work: complete independent review of the renderer, geometry, PCAPNG, SQLite-sidecar and Wi-Fi-signal units; integrate only approved changes, then implement the native receipt-time survey policy and typed spatial aggregation contract. Extend supply-chain coverage to the remaining distributable environments and hosts without weakening the verified Rust CLI scope. Immutable analysis manifests and hash invalidation contracts are independently approved and integrated; artifact loading, job execution and cache storage remain open. Active loopback proof is independently approved: 20 tests and eight actual corrected macOS wrapper checks pass. The earlier false success is retained as rejected evidence. LAN/remote, other OS execution and full active-survey integration remain open.

Primary integration branch: `main`. Agents edit isolated worktrees. Meaningful changes require a separate review before integration; no feature may be promoted on author tests alone.
