# Kyberia implementation status

The authoritative specification is [plan.md](plan.md). This is an active implementation, not a completed product.

Current integration checkpoint: independently approved cumulative merge and
ordering budgets through source commit `a29967a` are integrated. Full workspace
regression passes 590 tests, zero failures and nine ignored tests
(`.tools/merge-integration-regression.log`). Focused all-target Clippy, formatting,
architecture, source inventory and source-qualified ledger checks pass. See
[merge review](docs/reviews/budgeted-operation-merge-review.md). Stored RSSI scene
CLI export is under independent review in its isolated worktree; renderer
harness integration and hostile-storage preallocation fixes remain in progress.
These increments do not complete Phase 0 or the user-facing mapper.

Current review checkpoint: bounded polygon booleans are independently reviewed
and integrated through `21f1e16`; all 30 geometry tests pass. Full Gate E remains
open. Capture diagnostics and separated drain-test deadlines are integrated
through `93317f2`; 33 unit and two external tests pass, with one real-collector
test ignored. The reviewed pure materializer is integrated through `7427cb1`;
its focused dependency suites pass 247 tests with two ignored. Transactional
canonical project publication is now in progress in an isolated worktree.
These increments do not constitute a completed product capability or phase.

Publication draft checkpoint: the isolated
`.worktrees/materialized-project-publication` draft now includes explicit
baseline registration, historical retry validation, corruption checks and legacy
missing-state reads. Its full project-store suite passes 116 tests with zero
failures and one ignored test (`.trash/storage-draft-regression-7.log` within that
worktree); focused Clippy passes. Independent review requested changes; follow-up
confirms result-to-replay comparison and inventory reuse, but aggregate Rust-side
verification work remains a MAJOR finding. Baseline read access and registration
recovery tests now pass. The shared-budget prerequisite is assigned in the isolated
`.worktrees/shared-materialization-budget` worktree. ADR and final traceability
updates remain open. These results do not replace the integrated
workspace validation below and the draft is not yet approved for integration.

Additional isolated-draft validation: publication now passes the full Rust
workspace with 538 tests, zero failures and nine ignored tests
(`.worktrees/materialized-project-publication/.trash/publication-workspace-regression-3.log`).
The run required local TCP listener access for existing Kismet transport tests;
the earlier sandbox bind failures are retained in the preceding log. A minimal
observation-pipeline error mapping handles the new materialization error variant.

Renderer-neutral scene adapter through `39f6555` is independently approved and
integrated. Full workspace regression passes 585 tests, zero failures and nine
ignored tests; log: `.tools/rendering-scene-integration-regression.log`. It preserves canonical samples, numerically replays
imports and direct tiles, bounds predecode shapes/text, rejects duplicate fields,
and supports cancellation. Current-host memory/latency evidence is recorded in
[the resource review](docs/reviews/render-scene-resource-review.md). This is an
adapter increment; renderer selection, UI integration and Gate B remain open.
Shared materialization consumer integration now includes independently reviewed
operation admission, conflict/resolution copies, replay, identity serialization
and materializer budgets through source commit `b5dec0c`. The immutable combined
consumer suite passes 96 tests. Independently approved writer regressions
`7ad38e8` and `06db93a` are included too; both fail when budget checks are moved
after destination writes. Integrated workspace validation passes 550 tests,
zero failures and nine ignored tests; the retained log is
`.tools/budget-consumer-integration-regression.log`. Scoped all-target Clippy,
formatting, architecture and source inventory checks pass.

Reviewed prerequisite: the dependency-free `kyberia-resource-budget` primitive
is integrated with six passing tests and explicit local/shared failure atomicity.
See [scoped approval](docs/reviews/resource-budget-primitive-review.md). Its
operation-log, identity and materializer consumers are now integrated and tested.
Transaction-wide
storage consumption remains under review in its isolated draft.

Latest integrated validation at `8e68192`: `cargo test --workspace --locked
--offline` passes 530 tests, zero failures and nine ignored tests. The retained
log is `.tools/resource-budget-primitive-regression.log`. The primitive's Clippy,
formatting, architecture and source-inventory checks also pass. Consumer drafts
and their open review findings are excluded from this integrated result.

Integrated validation at `a11ca22`: `cargo test --workspace --locked --offline`
passes 524 tests with zero failures and nine ignored; workspace Clippy with
`-D warnings` also passes. Logs are retained in
`.tools/causal-materializer-full-regression.log` and
`.tools/materializer-integration-clippy.log`. Ignored runtime gates remain open.

- Current phase: **Phase 0 — Research harness and architecture proof**.
- Current iteration: **4 — Integration audit remediation, RF semantics, renderer/geometry proofs and supply-chain validation**.
- Completed foundation increments: independently reviewed canonical baseline/operation-set identities (`f9b4288`) and typed SQLite operation replay (`5eefe02`); reviewed bounded native collector session supervision with privacy/interface binding and retained test artifacts; reviewed revision-bound stored survey/observation-to-RSSI tile composition; independently reviewed method-specific nearest/IDW observed-RSSI metrics with preserved point-value artifact identity and unknown-gap contracts; independently reviewed canonical project/site name commands with exact inverse receipts and geometry-preserving replay; independently reviewed native normalized-observation persistence pipeline with typed capture manifests, recoverable publication and canonical survey-association closure; independently reviewed observed-RSSI selection bridge with exact BSSID, strict/receipt evidence validation, bounded source-binding manifest and numerical tile composition; independently reviewed bounded indexed observation-ID queries with selected-chunk integrity validation, exact selected-chunk provenance receipts, revision consistency, cancellation and future revision rejection; independently reviewed offline KismetDB canonical metadata normalization with atomic no-follow source acquisition; independently reviewed CLI export of verified normalized-observation Parquet with a closed versioned manifest and non-overwriting publication; independently reviewed immutable fixed-schema Parquet observation chunks with transactional SQLite publication, content hashes, cancellation visibility, bounded hostile-input decoding, PyArrow dataset interoperability and combined storage migration; independently reviewed transactional exact-byte operation-log persistence with additive migration, deterministic replay, semantic admission, bounded hostile-row preflight and projection recovery; independently reviewed canonical metric registry with bounded semantic definitions, shared UI/compute identity, strict legacy artifact migration, dimensional checks and enforced spatial-method binding; independently reviewed immutable operation-log encoding, bounded offline DAG merge, executable undo/redo and explicit conflict-resolution contracts; independently reviewed versioned Wi-Fi channel geometry, conservative puncturing admission, spectral coupling and effective-interference evidence; independently reviewed transactional immutable point-survey snapshot persistence with complete bounded index/history replay, typed source identity, legacy receipts and defensive preflight; independently reviewed bounded renderer Gate B evidence comparing pinned OpenLayers and custom WebGL2 on source-bound desktop/DPR2 workloads without selecting a product renderer; independently reviewed typed spatial signal aggregation with exact canonical metric-definition binding and tile-v2 provenance; independently reviewed receipt/API-window point association that cannot fabricate capture context or advance strict readiness; independently reviewed deterministic signal aggregation and fundamental linear-power/SNR/SIR/SINR arithmetic; independently reviewed immutable spatial-analysis manifests, canonical hashes and artifact-byte verification; independently reviewed lossless source ledger and execution DAG; unit-safe IDs/physical values, explicit unknowns, timing, covariance and observation contracts; 24 original scientific scenes, 25-point deterministic survey, clean-room TIN oracle and runtime evidence checker. These do not constitute a usable survey application or completion of Phase 0.
- In progress: polygon boolean operations following the independently approved planar adapter integrated through `b647d6f` (nine focused tests pass; full geometry gate remains open); reopened FND-011 operation-log/undo completion for typed irreversible/unknown prior states, causal inverse validation and baseline-bound canonical materialization; Kismet real-server and automatic DNS acquisition gates after the [reviewed TLS deadline correction](docs/reviews/kismet-live-status-review.md); Stored-analysis Unix CLI is independently reviewed and integrated through `305a1af`; desktop product workflow integration remains open. native collector product wiring and live acquisition spool, Kismet real-server status validation and PCAPNG normalization parity; Complete distributable supply-chain coverage, final renderer selection and native-product validation, production geometry integration, PAS-001 source/spatial integration, live observation spool and analytical query paths, canonical project materialization and coordinator transport, expansion of the canonical registry beyond its first observed-RSSI metric, and remediation of the iteration-wide independent audit. Gate B now has independently approved provisional current-host evidence for 120 sequential numerical layers, 10,000 AP/path overlays, bounded tile scheduling, explicit unknown masks and a six-floor custom extrusion proof; Tauri/WebView, a second OS/driver, tile compositing/network behavior, accessibility, product thresholds and final selection remain open. Gate E now has independently approved provisional evidence for a bounded portable planar Rust geometry boundary, explicit GEOS/Shapely repair adapter, and desktop/WASM behavioral parity; production 3D/CRS/CAD/material geometry and durable gate closure remain open. The pure Wi-Fi semantics package implements six explicit aggregation methods, evidence-strict SNR/SIR/SINR, standardized bonded centers, a conservatively pinned puncturing subset, asymmetric spectral coupling, typed utilization, identity deduplication and linear effective interference; sensor normalization, raw persistence, manifest binding, spatial selection, calibrated masks, complete regulations, CCA/airtime and final PHY/capacity remain open. The SQLite sidecar budget defect is corrected and independently approved; native macOS-to-point-survey admission and spatial aggregation-provenance drift remain open. The audit's PCAPNG section-length finding was disproved by the IETF specification and an independent BLOCKER review: main already uses the correct SHB-excluding field semantics, and the nonconforming proposed patch is retained but will not be integrated. The pinned Rust CLI SBOM/advisory tooling is independently approved and its committed-source macOS ARM64 execution passes; Python/Sionna/Kismet/controller and other host distributions remain open. The bounded active-process research boundary is independently approved after lifecycle and UDP-source corrections. Native capture normalization is independently approved after two API-timing corrections; the midpoint audit’s silent storage-write defect is corrected and independently verified. The isolated Sionna CPU proof and source-response timing contract are independently approved. The real SQLite/Parquet/DuckDB comparison is now independently approved after its required-provenance correction. Independently approved increments now include the point-survey state machine, immutable KismetDB metadata reader, bounded PCAPNG container adapter, numerical nearest/IDW tiles, observation/snapshot V2 migration, native macOS process, dependency checks and developer commands. These remain backend/proof increments; user-facing survey and canonical adapter wiring are open. Every agent has an isolated worktree within `.worktrees/`.
- Blocked capabilities: no whole capability is classified blocked. Specific unavailable hardware/OS/credential validation gates will be recorded separately in [BLOCKERS.md](docs/implementation/BLOCKERS.md).
- Validation status: identity correction through `f1775e9` passes the complete locked/offline Rust suite (477 passed, 0 failed, 9 ignored), with retained log `.tools/identity-correction-regression.log`; formatting also passes. Previous integrated baseline: integrated identity and typed storage replay at `5eefe02` pass `cargo test --workspace --locked --offline` (474 passed, 0 failed, 9 ignored); Clippy with warnings denied, formatting, architecture and ledger checks pass. Retained log: `.tools/materialization-integration-regression.log`. Historical validation: integrated V2 contracts at `8b340eb` pass the complete locked/offline Rust suite (460 passed, 0 failed, 9 ignored), workspace lint/architecture and refreshed ledger checks. Retained test log: `.tools/v2-integrated-regression.log`. Earlier validation: combined CLI/geometry regression at `5faa76a` passes 451 tests with 9 explicit ignores, plus the 241-package dependency audit (advisories, bans, licenses and sources). A sandbox-denied loopback run is retained separately from the successful authorized rerun in [validation](docs/validation/planar-geometry-candidate.md). Earlier validation: at `a2cc2b2`, a fresh `cargo test --workspace --locked --offline --quiet` run passed, as did `python3 tools/dev.py lint` (formatting, workspace Clippy with warnings denied, architecture) and `python3 tools/ledger.py check` (5,392 blocks, 438 ID occurrences, 446 headings). These checks cover the integration branch, not the pending isolated candidates. Historical validation: canonical-name integration passes 400 Rust tests including doctests (8 explicit benchmarks ignored). the Kismet integration passes 398 Rust tests including doctests (8 explicit benchmarks ignored), 179 Python tests (19 optional skips), workspace lint/typecheck and architecture; reviewed source-ledger hashes were refreshed and ledger/fixture checks pass. the renderer proof passes 8/8 deterministic Node tests, JavaScript syntax checks, a 72-package offline inventory check, and independently rerun desktop/DPR2 Chromium matrices with all 16 rows ready, 120/120 layers exercised per row, zero page errors and exact retained hashes. The integrated locked/offline Rust workspace passes 373 tests and eight compile-fail doctests. The operation-log package contributes 22 focused tests, including bounded hostile ancestry and a 100,000-root equal-effect frontier. Spatial analysis contributes 31 focused tests and Wi-Fi semantics 29, including canonical metric-definition bytes and deterministic registry identity, literal legacy wire/hash compatibility, unit/range and dimensional checks, enforced spatial methods, tile-v2 provenance equality, explicit temporal-method rejection, independent numerical oracles, exact-bit permutation invariance, finite extremes, unknown reasons and resource bounds. The survey package contributes two unit tests, nine receipt-association tests and 24 other passing tests, with one explicit benchmark ignored; the review exercised strict-time isolation, unknown preservation, replay policy, identity uniqueness and resource bounds. Workspace Clippy with `-D warnings`, architecture, source inventory and the 5,392-record ledger pass. The storage package contributes 101 tests (12 unit, 13 bundle, 26 observation-chunk, 17 operation-store, 13 schema-guard and 20 survey-snapshot), including exact combined SQLite main/sidecar limits, overflow, valid WAL-only state, bounded rollback behavior, and the rejected 86.5 MiB WAL bypass. The last complete `python3 tools/dev.py check` baseline also passes 179 Python tests with 19 optional native/analytical-engine/real-tool tests skipped, formatting, Clippy, typecheck, architecture, source and ledger checks. Six explicit Rust benchmarks and a deliberate golden-regeneration command are skipped in the default run; benchmarks pass when invoked separately. The separate native macOS command passes all 21 collector tests including those five native lifecycle tests. The optional real storage environment separately passes all 15 tests and nine benchmark cases. The source checker verifies 181 locked external packages. The committed-source Rust CLI SBOM contains 45 components and 46 dependency nodes, is byte-identical on repeat generation, validates against pinned official CycloneDX schemas, and passes cargo-deny advisories/licenses/bans/sources against RustSec `8a1eb4f`. Separately, the Sionna audited CPU environment passes 34 author checks and 19 independently rerun upstream tests; independent asymmetric path/map probes pass. The complete Sionna runtime gate remains open. Actual redacted CoreWLAN probing detects one interface outside the sandbox; authorized RF measurements remain open pending operator consent and field validation.
- Latest reviewed source commit: `a29967a` adds cumulative merge and ordering budgets after independent review; integrated workspace regression passes 590 tests with nine ignored. See [merge review](docs/reviews/budgeted-operation-merge-review.md).
- Earlier reviewed increments: identity resource-limit correction and V2 payload tests through `a01a00f` (15 focused tests pass on main; [review](docs/reviews/materialization-identity-resource-review.md)); trash-only clean command `55807d9`, independently approved in `c4dccb2`; bounded native supervision through `365a8e6`; stored RSSI composition through `64af05e`; Kismet DNS/TLS corrections through `f826a18`. V2 inverse/conflict/resolution contracts are reviewed and integrated through `8b340eb` in [review](docs/reviews/operation-inverse-v2-review.md); typed storage replay is reviewed and integrated through `5eefe02`; causal materialization remains open; CLI signal/publication findings are corrected and approved through `305a1af` in [review](docs/reviews/stored-rssi-cli-review.md). Historical reviewed increments: `b7b092f` completes the reviewed additive nearest/IDW RSSI metric contracts (46 focused tests passed; [review](docs/reviews/observed-rssi-interpolation-review.md)); previous increments: `4482b36` adds independently approved canonical name commands (see [review](docs/reviews/project-name-commands-review.md)); `dc86505` closes the measured-selector synthetic admission defect; stored composition and the Kismet DNS/TLS corrections were subsequently reviewed and integrated as listed above. `1f7a943` closes independently reviewed bounded Kismet live status transport (see [review](docs/reviews/kismet-live-status-review.md)); `9f03337` closes independently reviewed native pipeline publication integrity (see [review](docs/reviews/native-pipeline-pending-review.md)); `3d9d059` closes strict evidence and metric admission in observed-RSSI bridge `f12fcb7` (see [review](docs/reviews/observed-rssi-selection-review.md)); `fa0ca70` adds independently approved observation query provenance receipts; `c2e210b` adds independently approved bounded indexed observation queries (see [review](docs/reviews/observation-query-review.md)); `51bb264` closes the source-open race in reviewed Kismet normalization `7e05adf` (see [review](docs/reviews/kismet-normalization-review.md)); `70d0276` adds independently approved normalized observation Parquet export (see [review](docs/reviews/observation-export-review.md)); `3d91127` verifies the independently approved combined survey, operation and observation SQLite schema integration from `f976958` across all optional table-group combinations; `d3aec65` adds independently approved transactional operation-log persistence, replay-semantic admission, additive migration and recovery; `2c1248e` adds the independently approved canonical metric registry, strict legacy metric artifact compatibility and spatial-method binding; `34e689a` adds independently approved immutable operation-log encoding, bounded merge, undo/redo and conflict resolution; `761eb85` adds independently approved Wi-Fi channel geometry and coupling semantics; `9854e69` adds independently approved transactional survey snapshot persistence; `2881900` adds independently approved bounded renderer Gate B evidence; `6abe10a` adds independently approved verified spatial aggregation provenance; `aa4fbf4` adds independently approved receipt/API-window point association without weakening strict survey evidence; `29b4cf3` adds independently approved provisional planar geometry Gate E evidence; `109b067` adds independently approved deterministic Wi-Fi signal aggregation and fundamental RF power arithmetic; `fa4a7b2` bounds SQLite recovery sidecars and closes `STORE-ITER4-001`; `f834629` pins SBOM and dependency-audit tooling with committed-source evidence in `a9a28d6`; `7d9022c` is the bounded active-process proof; `cddd37b` adds immutable analysis manifests. Prior increments: `d6189d6` native capture normalization, independently approved in `21cd7e7`; storage correction `8f83a2f`, independently approved in `0d0ff97`; `b791ba0` Sionna CPU proof, independently approved in `7a10bb1`; source-response timing `f227b8b`, independently approved in `25b8249`; `6db6058` real storage comparison, independently approved in `24c2fe5` with separate correction review `86b860a`. Packet adapter `1eea541` is approved in `8ca1575` after correcting final-EOF cancellation. Spatial numerical code `c707ba3` is independently approved in `fc76fd7`.
- Known technical debt: Gate C remains Proposed pending sustained live workloads, process/disk failure injection, adversarial Parquet fuzzing, a second operating system, production analytical queries and distribution-footprint evidence. The canonical metric registry currently contains observed Wi-Fi RSSI only; planned throughput, SINR, spectrum, capacity and custom metric families remain open. The normalized native batch persists through the receipt-association contract and reviewed bounded process supervision; live product wiring and acquisition spool remain open. Individual manifest metadata hash-mutation cases need broader regression coverage; full artifact loading and validated tile import remain open; canonical project materialization, authorization, coordinator transport and product conflict UX remain open; the legacy project-receipt replay path still grows quadratically and has not yet migrated to the bounded operation-log contract. Renderer, storage, capture, geometry, optimizer and integration runtime gates are unresolved. Source IDs collide between sections and must never be used unqualified.
- Next executable work: finish transaction-wide publication verification using the integrated shared budget, independently review it and rerun storage/workspace regressions. Add budget-aware public operation merge in the isolated `.worktrees/budgeted-operation-merge` worktree; preserve existing conflict and duplicate semantics. Finish renderer-scene cancellation and practical resource admission in its isolated worktree, then independently review the adapter before wiring canonical tiles into the renderer harness. The scoped publication current-pointer correction is independently approved in [its review record](docs/reviews/materialized-publication-pointer-review.md); the full publication draft remains unapproved. Polygon boolean corrections and the pure causal materializer are already reviewed and integrated as recorded above. Carry the provisional planar geometry decision into a bounded production adapter without implying 3D/CRS/CAD support. Extend supply-chain coverage to the remaining distributable environments and hosts without weakening the verified Rust CLI scope. Immutable analysis manifests and hash invalidation contracts are independently approved and integrated; artifact loading, job execution and cache storage remain open. Active loopback proof is independently approved: 20 tests and eight actual corrected macOS wrapper checks pass. The earlier false success is retained as rejected evidence. LAN/remote, other OS execution and full active-survey integration remain open.

Primary integration branch: `main`. Agents edit isolated worktrees. Meaningful changes require a separate review before integration; no feature may be promoted on author tests alone.
