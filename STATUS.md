# Kyberia implementation status

The authoritative specification is [plan.md](plan.md). This is an active implementation, not a completed product.

- Current phase: **Phase 0 — Research harness and architecture proof**.
- Current iteration: **4 — Capture integration, active process proof and supply-chain validation**.
- Completed foundation increments: independently reviewed lossless source ledger and execution DAG; unit-safe IDs/physical values, explicit unknowns, timing, covariance and observation contracts; 24 original scientific scenes, 25-point deterministic survey, clean-room TIN oracle and runtime evidence checker. These do not constitute a usable survey application or completion of Phase 0.
- In progress: Bounded active-process research, release dependency tooling and renderer comparison. Native capture normalization is independently approved after two API-timing corrections; the midpoint audit’s silent storage-write defect is corrected and independently verified. The isolated Sionna CPU proof and source-response timing contract are independently approved. The real SQLite/Parquet/DuckDB comparison is now independently approved after its required-provenance correction. Independently approved increments now include the point-survey state machine, immutable KismetDB metadata reader, bounded PCAPNG container adapter, numerical nearest/IDW tiles, observation/snapshot V2 migration, native macOS process, dependency checks and developer commands. These remain backend/proof increments; user-facing survey and canonical adapter wiring are open. Every agent has an isolated worktree within `.worktrees/`.
- Blocked capabilities: no whole capability is classified blocked. Specific unavailable hardware/OS/credential validation gates will be recorded separately in [BLOCKERS.md](docs/implementation/BLOCKERS.md).
- Validation status: `python3 tools/dev.py check` passes: 159 Rust tests (19 capture, 47 domain, 23 survey, 12 KismetDB, 17 PCAPNG, 16 spatial, 23 storage, two executable CLI workflows), seven compile-fail doctests, 110 Python tests with 18 optional native/analytical-engine tests skipped, formatting, Clippy, typecheck, architecture, source and ledger checks. Five explicit Rust benchmarks and a deliberate golden-regeneration command are skipped in the default run; benchmarks pass when invoked separately. The separate native macOS command passes all 21 collector tests including those five native lifecycle tests. The optional real storage environment separately passes all 15 tests and nine benchmark cases. The source checker verifies 97 locked external packages. Separately, the Sionna audited CPU environment passes 34 author checks and 19 independently rerun upstream tests; independent asymmetric path/map probes pass. The complete Sionna runtime gate remains open. Actual redacted CoreWLAN probing detects one interface outside the sandbox; authorized RF measurements remain open pending operator consent and field validation.
- Latest reviewed commit: `d6189d6` native capture normalization, independently approved in `21cd7e7`; storage correction `8f83a2f`, independently approved in `0d0ff97`. Prior increments: `b791ba0` Sionna CPU proof, independently approved in `7a10bb1`; source-response timing `f227b8b`, independently approved in `25b8249`. Earlier increments: `6db6058` real storage comparison, independently approved in `24c2fe5` with separate correction review `86b860a`. Packet adapter `1eea541` is approved in `8ca1575` after correcting final-EOF cancellation. Spatial numerical code `c707ba3` is independently approved in `fc76fd7`. See [iteration validation](docs/validation/ingestion-spatial-iteration-3.md).
- Known technical debt: full editor undo/merge and large-history replay remain open; metadata operation replay currently grows quadratically. Renderer, storage, capture, geometry, optimizer and integration runtime gates are unresolved. Source IDs collide between sections and must never be used unqualified.
- Next executable work: complete the actual loopback active-process comparison, pinned SBOM/advisory commands and renderer performance comparison. Two isolated Luna xhigh agents own the supply-chain and renderer tasks at the user’s request; independent review remains required.

Primary integration branch: `main`. Agents edit isolated worktrees. Meaningful changes require a separate review before integration; no feature may be promoted on author tests alone.
