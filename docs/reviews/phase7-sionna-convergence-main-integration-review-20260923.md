# Independent review — integrated Phase 7 convergence candidate

**Candidate:** `c2f7a95305577a9843c82975d543df4673e010c9`

**Mainline base:** `9b9a136`

**Verdict:** APPROVE THE INTEGRATION CANDIDATE — no scoped findings; Phase 7 and runtime/product acceptance remain open.

## Ten-field handoff

1. **Objective and verdict:** Review the integrated Phase 7 convergence-support candidate, verify current-main documentation/ledger reconciliation, retain the F1/F2 cancellation guarantees, and ensure existing Phase 2 association/reassociation and Phase 5 band-power tracking was not lost. The bounded integration candidate is approved with no scoped BLOCKER/MAJOR finding; this is not Phase 7 completion or Sionna runtime acceptance.

2. **Plan and ledger anchors:** Read the full authoritative `plan.md` (SHA-256 `1e308d236d63520b6569243f09b1e1f3daf62e1e724f3993b4744af8c27dc6a6`; unchanged from the previously full-read `bc51e80` base), and reread §8.5, §16.13, Gate I, Phase 7 deliverables/exit criteria, and Appendix I convergence job type. Checked source IDs `source:heading:66df6bddc47f2e0c0973:1`, `source:list_item:5269a81e5d04ce55d10d:1`, `source:list_item:5fa9c2753492416e5529:1`, `source:list_item:eb7936ab43f4560d972d:1`, `backlog:PREB-011:1`, and `audit:PRE-012:1`. They remain `IN_PROGRESS`.

3. **Base and head:** Reviewed exact integration snapshot `c2f7a95305577a9843c82975d543df4673e010c9`, based on mainline `9b9a136` and containing source commits `a83ec42`, `f74595d`, and `0a0ee26`, with review reports `fe060de`, `4b5da95`, and `ed3b53c`, followed by reconciliation commit `c2f7a95`. The source correction retains the final-review base/head relationship `f25667a..4324275`.

4. **Worktree and branch:** Reviewed in `/private/tmp/kyberia-phase7-convergence-main-review-20260923`, detached at `c2f7a95`; the initial worktree was clean. The repository `main` ref remains at `9b9a136`, so this verdict approves the assembled integration candidate and does not claim the actual main pointer has moved. No integration or author tree was edited.

5. **Inspected and changed paths:** Inspected the full convergence helper and focused tests, relevant existing client/contract cancellation and envelope behavior, the integration commit diffs, `STATUS.md`, ledger, generated `TRACEABILITY.md`, validation note, and all three linked Phase 7 review reports. The Phase 7 integration diff from `9b9a136` touches only STATUS, ledger/traceability, review/validation documentation, the new helper, and its tests; no Phase 2 or Phase 5 implementation/test path is changed. This report is the only file changed by this review.

6. **Cancellation, failure, bounds, and provenance:** `run_sweep` passes the same event-like token to each existing client call and checks before dispatch. A metadata-valid failed `error="cancelled"` envelope with no cleanup/cancel diagnostic maps to `ConvergenceCancelled` only when the caller token is set. Failed envelopes carrying cleanup/cancel fields raise `ConvergenceWorkerFailure` with the original envelope and diagnostics retained; exceptions from the client propagate unchanged. The tests cover pre-cancel, in-call token propagation, between-run stop, cleanup/containment diagnostic preservation, and simultaneous client exceptions. I independently injected a failed `process_error` envelope while setting the token: `ConvergenceWorkerFailure` retained `process_error`, `Windows containment unknown`, and `terminate failed`, kept the envelope attached, and dispatched exactly one client call. The unchanged helper still validates request/result hashes, result contract and stable runtime/scene provenance. Existing per-request timeout and caps remain 8 budgets, 8 seeds, 32 runs, 4,096 cells/map, 100,000 summary scalars, 32,000,000 aggregate transmitter-samples, and 8 MiB serialized output.

7. **Checks:** `python3 -m unittest discover -s tests -p 'test_sionna_convergence.py' -v` — 14 passed. `python3 -m unittest discover -s tests -p 'test_ledger.py' -v` — 31 passed. `python3 tools/ledger.py check` — PASS (5,396 source blocks, 438 explicit ID occurrences, 447 headings). `python3 tools/architecture.py check` — PASS. `python3 tools/source_inventory.py check` — PASS (522 locked external packages). `git diff --check 9b9a136..c2f7a95305577a9843c82975d543df4673e010c9` — PASS. Current helper/test/validation hashes match the ledger.

8. **Status, Phase 2/5 preservation, and traceability:** Phase 7, Gate I, PREB-011, PRE-012, and convergence obligations remain `IN_PROGRESS`; validation explicitly says no worker/Sionna execution, field result, calibrated uncertainty, P2/P1 holdout, Wi-Fi SINR/capacity, or Phase 7 exit was established. The generated traceability totals are 129 `IN_PROGRESS` and 3,141 `NOT_STARTED`; five new leaf obligations are `IN_PROGRESS` under the Phase 7 group. Comparing the 5,396 ledger IDs against `9b9a136` found no removed or added IDs; all nine path-linked Phase 2/5 ledger records compare unchanged. `STATUS.md` still records association/reassociation framing (`fefcce6`, review `3737372`) and whole-bin band power (`eb27cbf`, review `8406e06`), and their Phase 2/5 status rows remain present. All three Phase 7 review artifacts referenced by the reconciled validation/status exist in this candidate. Graph MCP was unavailable; no graph/coverage claim is made.

9. **Report commit and cleanliness:** Report-only commit is recorded in the reviewer handoff. Candidate product, status, ledger, and traceability files were not edited by this review; the reviewer worktree is verified clean after committing the report.

10. **Remaining risks and limitations:** The approved code/tests remain synthetic support evidence only; no pinned Sionna worker/runtime, numerical convergence, cross-host performance, field calibration, held-out P2/P1 comparison, or product acceptance was run. The repository `main` ref is still `9b9a136` at review time; merge/promotion of this already reconciled candidate is a separate repository operation, not performed here.
