# Phase 1 manual-path work-budget follow-up re-review

1. **Outcome: APPROVE.** The follow-up resolves the prior MAJOR unbounded-work finding. I found no new in-scope blocker or major issue in the 3,000,000-unit budget implementation.

2. **Reviewed candidate:** `91060ef3bbdcfd779ad8fb4a28b06609810c46e0`, parent `b6e46621de2c92cd48b273a6a5e82833c1e2f02e`. Product-source review was read-only in `/private/tmp/kyberia-phase1-manual-path-rereview-91060ef-20260923`; this report is committed separately from `/private/tmp/kyberia-phase1-manual-path-rereview-report-91060ef-20260923`.

3. **Plan scope:** SUR-002, *Manual continuous-path survey*, in `plan.md` §6.3 and the Phase 1 roadmap; MAPB-004. This re-review focused on the added channel-gap CPU-work bound and its documentation/tracking, not on closing the broader survey requirement.

4. **Files and symbols reviewed:** `ChannelGapWork::charge` and `sort_channel_intervals` in `crates/survey/src/manual_path.rs:15-67`; `ManualPathSurvey::channel_gaps`, `check_interval`, and `push_channel_gap` at `:910-1034`; the regression `dense_tied_complete_coverage_fails_closed_at_total_work_bound` in `crates/survey/tests/manual_path.rs:467-505`; and the changed `STATUS.md`, `docs/implementation/ledger.json`, `docs/architecture/survey-state.md`, and `docs/validation/manual-continuous-path.md`.

5. **Method and checks:** I inspected the complete follow-up diff and surrounding code directly. Graph/index MCP was unavailable, so this is source-qualified inspection only and makes no graph coverage/completeness claim. Independently passed: `cargo test --locked --offline -p kyberia-survey` (47 passed, 1 pre-existing ignored release benchmark), `cargo clippy --locked --offline -p kyberia-survey --all-targets -- -D warnings`, `cargo fmt --all -- --check`, `python3 tools/ledger.py check`, `python3 tools/architecture.py check`, `python3 tools/source_inventory.py check` (522 locked external packages), and `git diff --check b6e46621de2c92cd48b273a6a5e82833c1e2f02e 91060ef3bbdcfd779ad8fb4a28b06609810c46e0`.

6. **Findings:** No unresolved finding in this follow-up. The former MAJOR case is now charged against a single counter shared across the entire call. Counter addition is overflow-checked; the next operation is rejected before execution if it would exceed the limit, and `Err(ManualPathError::Limit)` returns no partial vector or mutates the survey. Normal gap ordering and meanings remain unchanged.

7. **Work-bound analysis:** The cap is checked before schedule/coverage interval validation, each coverage filter visit, every insertion-sort comparison and move, schedule/anchor-pair visits, every coverage/segment scan, and each emitted gap. Input vectors, path anchors, and result count also retain their existing limits. The regression constructs 511 path segments, 8 schedules, and 2,048 same-start `Complete` intervals whose nested union covers the full `[0, 511s)` scheduled interval. Because coverage is complete, the result would contain zero gaps; the sorted scan needs about 4.19 million coverage/segment visits, so it must return `Limit` without relying on the 16,384-gap output cap. Text copied into results is itself bounded (`Text` is at most 1,024 bytes) and the output vector remains capped. The counter is a deterministic operation budget, not a wall-clock deadline.

8. **Ledger and documentation:** The generated ledger check passes. `catalog:SUR-002:1`, all five SUR-002 requirement leaves, and `backlog:MAPB-004:1` remain `IN_PROGRESS`. The source hash for `manual_path.rs` and the test/architecture/validation hashes recorded in the validation note match the candidate. `STATUS.md` continues to state that application/storage integration, map HUD, field usability, and Phase 1/SUR-002 acceptance are open.

9. **Limitations and integration note:** Graph evidence was unavailable; findings rely on exact source inspection and the checks above. The candidate is based on `afc5070`, while current main `7734758` adds the separate point-survey application/session API. The Rust source changes are disjoint, but `STATUS.md` and the generated ledger are shared files: this candidate ledger does not contain current main's `crates/application/src/session.rs` entry for SUR-001. Reconcile/regenerate these files on the integration branch and rerun their checks; this is an integration bookkeeping requirement, not a defect in the budget fix. The model remains a foundation only, not a Phase 1 exit.

10. **Report artifact:** This report is committed separately in the reviewer worktree; the assigned candidate source worktree remains unchanged.
