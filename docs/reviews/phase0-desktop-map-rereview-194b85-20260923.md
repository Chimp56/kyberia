# Independent re-review: Phase 0 desktop map workflow correction

1. **Verdict and scope** — **REQUEST CHANGES; not approved for integration yet.** The two prior source findings are resolved: the renderer now retains a committed receipt and exposes a query-only recovery action, and application idempotent import lookup precedes fresh-frontier derivation. The newly added Playwright recovery regression fails before reaching that behavior because its injected mock references an out-of-scope variable. A minor validation-document plan-anchor typo is also noted. This review covers the exact candidate source and focused checks; it is not product, platform, or graph-completeness approval.

2. **Plan IDs and anchors** — Read the complete authoritative `plan.md`; reviewed §5.3; MAP-002 and MAP-003 in §6.2 (plan lines 803–831); architecture/process and domain boundaries in §§10.1–10.7; security/threat model §15; validation §16; Phase 0 deliverables and exit criteria in §17 (lines 3987–4015); and MAPB-001/MAPB-002 in §18.4 (lines 4264–4269). This candidate remains only a bounded PNG-container import and numeric two-point-calibration increment. The formats, preview, multi-point/CRS/orientation/residual, survey integration, two-platform determinism, live capability probing, and raw export requirements remain open.

3. **Exact revisions** — Candidate `194b85b0027f482b68de20a38f87179841627a9a` (tree `c22b93c417dcda49e55a9345ff98f2b9f304d662`); base and candidate merge-base `bc51e80b14e30f927628f4ba9f2e92a4773423fe`; later current main reviewed read-only at `afc5070fadac46411f02d355a0d00351f388205f`.

4. **Review worktree** — `/private/tmp/kyberia-phase0-desktop-map-rereview-194b85-20260923`, initially clean at the exact candidate HEAD. Product/source, STATUS, ledger, architecture, and current-main files were not edited. The targeted Playwright screenshot/trace and Vite cache were retained in this review worktree under ignored `apps/desktop/.trash/test-runs/`; the temporary dependency symlink was moved back into ignored `.trash`. The pre-existing root Vitest cache SHA-256 remained `06128d1a64f4f9eeee6eaff74b603f1880a8194d4f75508bd72bc99aa959b745`.

5. **Reviewed paths** — The full candidate diff includes Tauri shell/IPC, React workspace/components/contracts/state, application commands/session/map mutation/ports, domain project hierarchy, Cargo metadata, ADR 0035, validation packet, STATUS, TRACEABILITY, ledger, and license inventory. Focused re-review covered `crates/application/src/{map_asset.rs,port.rs,map_mutation.rs}`, `crates/application/tests/project_session.rs`, `crates/domain/src/project.rs`, `crates/operation-log/src/lib.rs`, `crates/causal-materializer/src/lib.rs`, `crates/project-store/src/operation_log.rs`, `apps/desktop/src-tauri/src/{lib.rs,main.rs}`, `apps/desktop/src/lib/{useProjectSession.ts,ui-state.ts,ui-state.test.ts}`, `apps/desktop/src/components/{CanvasStage.tsx,InspectorPanel.tsx}`, `apps/desktop/tests/e2e/desktop-shell.spec.ts`, and the governance/validation files named above.

6. **Invariants and findings** —

   - The two earlier findings are resolved in source. Import intents now check the existing operation identity and exact immutable payload/provenance/artifact bytes before deriving a new context (`crates/application/src/port.rs:183–225`). The reopen-and-retry application fixture now constructs nine concurrent heads and verifies exact import and calibration retries (`crates/application/tests/project_session.rs:1156–1244`); it passes independently. For readback recovery, the renderer stores the full durable receipt plus project ID, marks the cached projection in error, gates map/calibration actions, and only clears recovery when a current-project query returns the same project at a revision at least the receipt revision (`apps/desktop/src/lib/ui-state.ts:18–28,91–113`; `useProjectSession.ts:238–287,316–320,510–528`; `App.tsx:17–18,57–58`). Lagging reads leave the recovery action available. The targeted E2E fixture is intended to assert two queries and exactly one import (`desktop-shell.spec.ts:328–397`).

   - **MAJOR — the new rendered recovery regression fails before exercising recovery.** `baselineResponse` is a module-scope variable (`desktop-shell.spec.ts:3`), but the function passed to `page.addInitScript` is serialized into the browser and does not capture that module scope. Its callback receives only `{ recovered, staleProject }` at lines 345–382, yet `createBlankProject` returns the unbound `baselineResponse` at line 356. Independent run failed with browser error `baselineResponse is not defined` and then timed out waiting for “Choose PNG floor plan” at line 385. Pass the baseline response as an explicit `addInitScript` argument (or return the already-in-scope `staleProject`) and rerun the rendered regression before integration.

   - **MINOR — incorrect plan section in the validation packet.** `docs/validation/desktop-map-workflow-current-main.md:15` labels MAP-003 as §6.3; the authoritative plan places both MAP-002 and MAP-003 in §6.2 (`plan.md:781–831`, MAP-003 heading at 822). Correct the reference when the packet is next updated; TRACEABILITY’s source anchor itself is under the correct plan heading.

   - The PNG admission code is byte-for-byte unchanged across base, candidate, and current main. `crates/application/src/map_asset.rs:63–267` checks signature, chunk syntax/length/CRC, strict supported ordering, dimensions/pixels/chunk/metadata bounds, and a narrow ancillary set: gAMA/sRGB before optional PLTE, tRNS after PLTE whenever PLTE is present and before IDAT with color-type-specific lengths/palette bounds, and pHYs before IDAT; IDAT chunks must remain contiguous. Its fixtures create CRC-valid chunks (`:325–331`) and cover the supported Table-7 order and malformed order/palette cases (`:458–519`). It does **not** validate IDAT zlib/DEFLATE, inspect pixels, or establish that an admitted PNG is decodable/displayable; code comments and UI/ADR/validation text correctly preserve that limitation. `admitted_map` derives dimensions/hash from bytes; import stores `image/png`, source length, provenance and content hash and verifies the store hash (`port.rs:183–267`); duplicate retries compare the stored artifact bytes before returning the existing operation.

   - Native selection uses opaque, expiring, project/floor-bound, one-shot grants and opens regular files without following Unix symlinks or Windows reparse points (`apps/desktop/src-tauri/src/lib.rs:47–78,936–1089`). Staging checks cancellation while reading fixed-size chunks and enforces the 32 MiB bound and stable length (`:1163–1235`); native IPC does not return the filesystem path or raster bytes to the renderer. `canonical_map_state` cross-checks baseline, operation set, and store identity/count/revision and materializes the full operation set before new intent causality is derived (`port.rs:521–562`). A candidate is materialized/validated before revision-conditional append; after append, publication and exact readback are separate, and the Tauri response preserves the committed receipt if readback fails (`port.rs:289–376`; `lib.rs:1238–1251`). Budget/cancellation checks and structured public errors remain at the application/native boundaries.

   - Calibration intents validate map/floor frames and in-image control bounds, preserve exact prior active-calibration values for retries, and use V3 operations. The existing causal materializer detects unordered evidence-lock/floor-mutation conflicts by ancestry, independent of operation-ID order (`causal-materializer/src/lib.rs:1447–1480,2852–2916`), while the other-floor case remains independent (`:2918–3003`). Legacy V1/V2 operation/project handling remains in the unchanged schema/materializer paths; the new project hierarchy constructor adds a revision-zero floor baseline without replacing the legacy empty-project constructor.

7. **Independent checks** — PASS: focused offline five-crate Cargo tests (domain, operation-log, causal-materializer, project-store, application; 2 existing ignored tests); exact nine-head application retry test; Tauri tests (23 library, 6 binary, 2 IPC-boundary); strict Clippy for both Rust scopes; `cargo fmt --all -- --check`; `git diff --check`; architecture check; source inventory (522 packages); ledger check (5,396 source blocks); and ledger Python tests (31). TSC and Vitest independently passed at final candidate state (18/18 tests over 3 files) with npm/Vite caches inside this review tree; the root Vitest cache hash above is unchanged. **FAIL:** `npm --cache ../../.trash/test-runs/npm-cache run e2e -- --grep 'committed map receipt exposes a query recovery path without repeating import'` from `apps/desktop`, with the deterministic unbound-variable error detailed above. The author packet’s Windows check remains blocked before crate compilation by missing `x86_64-w64-mingw32-gcc`; Windows adapter compilation/runtime, native picker runtime, full Playwright suite, and two-platform determinism were not established. No online retry was made.

8. **Governance and current-main compatibility** — Candidate STATUS keeps the workflow unintegrated and Phase 0/1 open; candidate TRACEABILITY/ledger keep MAP-002, MAP-003, MAPB-001 and MAPB-002 `IN_PROGRESS`. No Phase 0 exit or pixel/display decoding is claimed. Candidate validation accurately said the new E2E had not yet been run, but should be updated with the fix and result. Against `afc5070`, map admission, operation-log, materializer and project-store operation-log blobs are identical to base. Main’s `domain/project.rs`, application command/session, and Tauri entry files also remain at their base blob; there is no map API/schema incompatibility requiring an unreviewed source port. The only candidate/current-main changed-path overlap after the base is `STATUS.md`, `docs/implementation/TRACEABILITY.md`, and `docs/implementation/ledger.json`; reconcile/regenerate these against current main instead of replacing them wholesale. The candidate is otherwise a bounded source replay/cherry-pick candidate after the failing E2E finding is resolved and governance files are reconciled. Graph/index resources were unavailable; this report makes no graph coverage or completeness claims.

9. **Report commit and cleanliness** — Report-only commit hash is recorded in the final handoff. Final worktree status is clean; only this review report was committed. No integration-main changes were made.

10. **Residual risks and blockers** — The new Playwright test is an integration-gate blocker until its fixture is passed into the browser callback and the recovery scenario passes. Actual native picker and Windows platform behavior remain unverified; Phase 0’s two-platform/live-capability/raw-export exits remain open. Admitted PNG data is container metadata only, not a decoded or displayable image. The plan-anchor typo is a minor documentation correction. No graph evidence is available.
