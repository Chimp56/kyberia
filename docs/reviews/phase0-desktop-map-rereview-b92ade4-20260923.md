# Phase 0 desktop map workflow follow-up review — b92ade4

## 1. Verdict and scope

**REQUEST CHANGES (minor evidence-record correction; no remaining blocker or
major finding in the reviewed follow-up).** This is an independent review of
the bounded renderer-fixture/documentation follow-up, not product acceptance.
The previously reported `addInitScript` fixture-scope defect and plan-anchor
typo are fixed, and the targeted Chromium regression passes independently.
Before integration, reconcile the stale validation summary in the source-
qualified ledger described in field 8. This verdict does not close Phase 0/1,
MAP-002, MAP-003, MAPB-001 or MAPB-002.

## 2. Plan IDs and anchors

Reviewed against the authoritative `plan.md` requirements in §5.3 (floor-plan
ingestion/calibration), §6.2 MAP-002 and MAP-003, Phase 0 §17 exit criteria,
§18.4 MAPB-001/MAPB-002, and the relevant §15 security/resource/test gates.
The updated validation packet correctly identifies MAP-002/MAP-003 as §6.2
(`docs/validation/desktop-map-workflow-current-main.md:13-24`). The full
feature remains a limited Phase 0 persistence slice, not an exit claim.

## 3. Exact revisions

- Candidate: `b92ade419c415ae48076b40ba937f351352d35c0`.
- Parent: `194b85b0027f482b68de20a38f87179841627a9a`.
- Requested current-main compatibility baseline: `afc5070fadac46411f02d355a0d00351f388205f`.
- The exact candidate and parent revisions were confirmed in the assigned
  detached review worktree before inspection.

## 4. Review worktree

`/private/tmp/kyberia-phase0-desktop-rereview-b92ade4-20260923`, detached at
the exact candidate. No product/candidate files or current-main files were
edited. The worktree is clean after committing this report.

## 5. Inspected paths

Reviewed the candidate follow-up diff and relevant source/evidence in
`apps/desktop/tests/e2e/desktop-shell.spec.ts`,
`docs/validation/desktop-map-workflow-current-main.md`, `STATUS.md`,
`docs/implementation/ledger.json`, `docs/implementation/TRACEABILITY.md`,
`plan.md`, and the current-main application/domain/operation-log/map-asset,
Tauri IPC and renderer workflow paths. Compared candidate/base/current-main
path changes and checked the retained Playwright `.last-run.json` generated by
the independent run.

## 6. Invariants and findings

- The regression fixture now passes `baselineResponse` as an explicit
  `addInitScript` argument (`desktop-shell.spec.ts:345-382`) instead of closing
  over an unserialized module binding. This removes the prior fixture failure
  (`baselineResponse` was not bound in the browser init-script context). The
  corrected scenario visibly retains committed-receipt recovery after a
  lagging read, then reconciles from a same-project current read
  (`:386-397`); it asserts two current-view queries and exactly one import.
- The plan anchor was corrected from §6.3 to §6.2 for MAP-002/MAP-003
  (`desktop-map-workflow-current-main.md:15-17`).
- Earlier source review invariants remain relevant and unchanged by this
  follow-up: import retries verify the committed operation/artifact before
  deriving a new frontier; the renderer retains the committed receipt and
  project identity, keeps stale recovery actionable, and does not retry the
  mutation; the native selection/staging path remains opaque, project/floor
  bound, no-follow, bounded and cancellable. The bounded PNG path is
  container/metadata admission only: IDAT is not inflated, pixels are not
  returned/rendered, and displayability is not established
  (`desktop-map-workflow-current-main.md:28-60`).
- No new security, causal, calibration, renderer or compatibility regression
  was found in the four-file follow-up. Findings are confined to the stale
  ledger evidence description/scope in field 8.

## 7. Independent checks and retained evidence

- Ran the exact targeted Chromium test independently from `apps/desktop`:
  `npm --cache ../../.trash/test-runs/npm-cache run e2e -- --grep 'committed map receipt exposes a query recovery path without repeating import'` — **PASS**, one test.
- Inspected the retained run file at
  `apps/desktop/.trash/test-runs/playwright-artifacts/.last-run.json`:
  `{"status":"passed","failedTests":[]}`.
- `git diff --check 194b85b0027f482b68de20a38f87179841627a9a..HEAD` — PASS.
- The author packet records Rust focused suites, Tauri suites, Clippy,
  formatting, TSC, Vitest, architecture, inventory and ledger checks as
  passing (`desktop-map-workflow-current-main.md:64-90`). These are recorded
  evidence, not independently rerun in this review. The complete Playwright
  suite/build, native picker runtime, Windows compilation/runtime and
  two-platform determinism remain open (`:110-114`).
- The root Vitest cache SHA-256 was
  `06128d1a64f4f9eeee6eaff74b603f1880a8194d4f75508bd72bc99aa959b745` before
  and after this run. Current-main status remained at its pre-existing state:
  `main` ahead of origin with only untracked `.pnpm-store/` and `package.json`;
  neither was changed.

## 8. Tracking and current-main compatibility

All tracked obligations remain open: MAP-002/MAP-003 are `IN_PROGRESS` in
`TRACEABILITY.md` (around lines 697 and 716), MAPB-001/MAPB-002 remain open in
the ledger, and `STATUS.md:51-66` explicitly says this adaptation is
unintegrated and does not close Phase 0 exit criteria.

The compatibility comparison against the requested `afc5070` baseline found
no later current-main changes to the relevant implementation contracts. The
candidate paths overlapping changes from `bc51e80` in current-main are
governance files (`STATUS.md`, `TRACEABILITY.md`, and `ledger.json`); these
must be reconciled with the live integration history rather than copied over
it. The follow-up ledger entry at `ledger.json:30509-30514` remains materially
stale: it says frontend cache isolation remains open, that frontend assertions
are stale and the shared Vitest cache was touched, while the validation packet
records final TSC/Vitest/targeted Playwright passes and an unchanged root cache;
it also says the Tauri suites were `22+6+2`, while the packet says `23+6+2`.
This is a minor evidence-traceability defect. Correct the description and
scope (or regenerate/reconcile the ledger) before integrating the governance
changes. Do not interpret the bounded workflow as pixel decoding/display,
field calibration, raw export, full format support, or Phase 0/1 completion.

Graph MCP resources/tools were unavailable; this review used exact Git/source
inspection and makes no graph-coverage or completeness claim.

## 9. Report commit and cleanliness

Report-only commit: to be recorded in the final handoff. Only this report is
staged/committed in the assigned review worktree; candidate and integration
trees remain untouched.

## 10. Remaining risks and blockers

No code-level blocker or major finding remains in this follow-up. The minor
ledger evidence mismatch should be corrected before integrating the status,
traceability and ledger changes. Product/runtime risks remain as enumerated in
the validation packet: full end-to-end coverage, native picker runtime,
Windows adapter validation, two-platform determinism, actual PNG decode and
display, broader calibration, raw export and Phase 0 exit evidence are absent.
