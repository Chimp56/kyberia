[package]
name = "kyberia-operation-log"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Pure deterministic versioned project operation log and offline merge contracts"

[dependencies]
kyberia-domain = { version = "=0.1.0", path = "../domain" }
kyberia-resource-budget = { version = "=0.1.0", path = "../resource-budget" }
serde = { version = "=1.0.228", features = ["derive"] }
serde_json = "=1.0.149"
sha2 = "=0.10.9"

[dev-dependencies]
proptest = "=1.10.0"

[lints]
workspace = true
