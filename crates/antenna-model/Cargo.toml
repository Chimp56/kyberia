[package]
name = "kyberia-antenna-model"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0 OR MIT"
description = "Versioned, bounded antenna-pattern model and gain evaluator"

[dependencies]
serde = { version = "=1.0.228", features = ["derive"] }
serde_json = "=1.0.149"
sha2 = "=0.10.9"

[lints.rust]
unsafe_code = "forbid"
