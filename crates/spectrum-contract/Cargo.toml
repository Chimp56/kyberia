[package]
name = "kyberia-spectrum-contract"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Versioned, validated spectrum evidence and pattern identity contracts"

[dependencies]
kyberia-domain = { version = "=0.1.0", path = "../domain" }
serde = { version = "=1.0.228", features = ["derive"] }
serde_json = "=1.0.149"
sha2 = "=0.10.9"

[lints.rust]
unsafe_code = "forbid"
