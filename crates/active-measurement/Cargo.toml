[package]
name = "kyberia-active-measurement"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Bounded consented TCP-connect active measurement contracts and adapter"

[dependencies]
kyberia-domain = { version = "=0.1.0", path = "../domain" }
sha2 = "=0.10.9"
mio = { version = "=1.2.3", default-features = false, features = ["net", "os-poll"] }

[dev-dependencies]
proptest = "=1.10.0"
serde_json = "=1.0.149"

[lints.rust]
unsafe_code = "forbid"
